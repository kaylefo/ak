import AdminStubPage from "../_components/stub-page";

export default function AdminListingsPage() {
  return (
    <AdminStubPage
      title="Listings"
      description="Review flagged listings, merge duplicates, and override classifications."
    />
  );
}
