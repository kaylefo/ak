import AdminStubPage from "../_components/stub-page";

export default function AdminParsersPage() {
  return (
    <AdminStubPage
      title="Parsers"
      description="Parser versions, drift detection, and schema mapping rules."
    />
  );
}
